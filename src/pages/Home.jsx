import SearchBar from "../component/SearchBar";
import ImageGrid from "../component/ImageGrid";

const Home = () => {
  return (
    <>
      <section>
        <SearchBar />
      </section>
      <section>
        <ImageGrid />
      </section>
    </>
  );
};

export default Home;
